<!doctype html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Profile</title>
</head>

<body class="bg-zinc-900 text-white">
    <div class="w-full min-h-screen p-6 sm:p-10">

        <!-- Logout button -->
        <a href="/logout"
            class="px-4 py-2 bg-red-600 hover:bg-red-700 transition rounded-md float-right text-sm font-medium">
            Logout
        </a>
        <h5 class="text-zinc-400 mb-6">Edit your post ✍️</h5>

        <!-- Post form -->
        <form method="post" action="/update/<%= post._id%>">
            <textarea name="content" rows="4" placeholder="What's on your mind?" required
                class="block w-full sm:w-1/2 p-3 resize-none bg-zinc-800 border border-zinc-700 rounded-md outline-none focus:ring-2 focus:ring-red-500"><%= post.content %></textarea>
            <input type="submit" value="Update Post"
                class="w-44 px-4 py-2 text-sm bg-yellow-600 hover:bg-yellow-700 transition block rounded-md mt-3 cursor-pointer">
        </form>



    </div>
</body>

</html>